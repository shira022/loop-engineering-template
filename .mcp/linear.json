{
  "mcpServers": {
    "linear": {
      "command": "npx",
      "args": [
        "-y",
        "@michaellattanzi/linear-mcp-server"
      ],
      "env": {
        "LINEAR_API_KEY": "${LINEAR_API_KEY}"
      }
    }
  }
}
